# Permanently deferred

Future `/improve` audits and agents **must not** re-open these as findings, direction items, spikes, or plans. The operator recorded this on 2026-08-26.

## Signed / notarized macOS builds

**Do not propose.** No plan, no spike, no “revisit after packaging,” no Apple Developer ID / `--osx-sign` / notarize / staple / hardened-runtime work.

Unsigned GitHub Release tarballs plus the README Gatekeeper / `xattr` / right-click Open instructions remain the install path. That is an accepted product and legal choice, not unfinished work.

If macOS distribution policy changes, a **human** will say so in a new request. Until then, skip this topic entirely.
